<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_app_gradient"
    android:fitsSystemWindows="true"
    tools:context=".Activities.MainActivity">

    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="108dp" />

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/nav_container"
        android:layout_width="match_parent"
        android:layout_height="76dp"
        android:layout_gravity="bottom"
        android:layout_marginHorizontal="18dp"
        android:layout_marginBottom="16dp"
        app:cardBackgroundColor="@color/bg_nav_floating"
        app:cardCornerRadius="30dp"
        app:cardElevation="14dp"
        app:cardUseCompatPadding="false"
        app:strokeColor="@color/nav_stroke"
        app:strokeWidth="1dp">

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_navigation"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:paddingHorizontal="6dp"
            app:itemActiveIndicatorStyle="@style/Widget.App.BottomNavigationView.ActiveIndicator"
            app:itemHorizontalTranslationEnabled="false"
            app:itemIconSize="23dp"
            app:itemIconTint="@color/bottom_nav_tint"
            app:itemRippleColor="@color/nav_ripple"
            app:itemTextAppearanceActive="@style/TextAppearance.App.BottomNavigation"
            app:itemTextAppearanceInactive="@style/TextAppearance.App.BottomNavigation"
            app:itemTextColor="@color/bottom_nav_tint"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/bottom_nav_menu" />
    </com.google.android.material.card.MaterialCardView>
</FrameLayout>
